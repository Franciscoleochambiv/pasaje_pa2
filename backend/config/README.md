# Configuración

Variables de entorno y configuración del backend.

Ejemplo para desarrollo (crear `.env` o exportar):
- `DB_HOST=localhost`
- `DB_PORT=5432`
- `DB_USER=postgres`
- `DB_PASSWORD=***`
- `DB_NAME=pasaje`
